{
  "name": "ai-agents",
  "version": "1.0.0",
  "description": "Multi-Agent AI System untuk Bisnis Digital",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node -e \"require('dotenv').config(); const {callAgent}=require('./utils/claudeClient'); callAgent('orchestrator',{OWNER_GOAL:'Test',BUSINESS_LINE:'semua',ADDITIONAL_CONTEXT:'test',TIME_BUDGET:'normal'}).then(r=>console.log('OK:',r.substring(0,100))).catch(console.error)\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "mysql2": "^3.22.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
